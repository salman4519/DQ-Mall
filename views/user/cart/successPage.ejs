<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <style>
        body {
            background-color: #f7f7f7; /* Light background for simplicity */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            flex-direction: column;
        }
        .success-container {
            text-align: center;
        }
        .checkmark {
            width: 150px; /* Size of the circle */
            height: 150px;
            border: 10px solid #28a745; /* Green border */
            border-radius: 50%;
            position: relative;
            display: inline-block;
            background: white; /* White background for the circle */
            margin-bottom: 20px; /* Space between checkmark and text */
            box-shadow: 0 4px 20px rgba(0,0,0,0.2); /* Shadow for depth */
            overflow: hidden; /* Hide overflow for circular effect */
        }
        .checkmark svg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            /* transform: rotate(45deg); */
        }
        .checkmark circle {
            fill: none;
            stroke: #28a745;
            stroke-width: 10;
            stroke-dasharray: 440; /* Length of the circle */
            stroke-dashoffset: 440; /* Start from the full length */
            animation: drawCircle 0.8s ease forwards;
        }
        .checkmark path {
            fill: none;
            stroke: #28a745;
            stroke-width: 12; /* Increased stroke width for the checkmark */
            stroke-linecap: round;
            stroke-dasharray: 75; /* Increased length for the checkmark */
            stroke-dashoffset: 75; /* Start from the full length */
            animation: drawCheck 0.5s ease forwards;
            animation-delay: 0.7s; /* Delay for better timing */
        }
        @keyframes drawCircle {
            to {
                stroke-dashoffset: 0; /* Draw the circle */
            }
        }
        @keyframes drawCheck {
            to {
                stroke-dashoffset: 0; /* Draw the checkmark */
            }
        }
    </style>
</head>
<body>
    <div class="success-container">
        <div class="checkmark animate__animated animate__zoomIn">
            <svg viewBox="0 0 100 100">
                <circle cx="50" cy="50" r="40"></circle>
                <path d="M30 50 L45 65 L70 35"></path> <!-- Larger checkmark -->
            </svg>
        </div>
        <div class="alert alert-success">
            <h2>Your order has been placed successfully!</h2>
            <p>Thank you for your purchase. Your order ID is <strong><%= orderId %></strong>.</p>
            <p>We will notify you once your order is dispatched.</p>
            <a href="/" class="btn btn-primary">Go to Homepage</a>
            <a href="/profile/orders" class="btn btn-secondary">View My Orders</a>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
